# Procfile enables you to declare a required runtime process called 
# process tpyes in which each line is a process type


# [PROCESS_TYPE]: COMMAND
# web: Process that handles http traffic
web: gunicorn LERT.wsgi:app
