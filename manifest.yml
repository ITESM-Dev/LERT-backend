---
applications:
 - name: LERT-Flask
   random-route: true
   memory: 500M
   disk_quota: 1G
   buildpacks:
      - https://github.com/cloudfoundry/python-buildpack.git  
   env:
      ENVIRONMENT: prod